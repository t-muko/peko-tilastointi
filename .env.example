# Firebase Web API key (safe to expose in browser app, but keep out of git history)
VITE_FIREBASE_API_KEY=replace_with_your_firebase_web_api_key

# Local emulator toggle for app startup
VITE_USE_EMULATOR=false
