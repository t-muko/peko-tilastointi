name: Deploy to Firebase Hosting on PR

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: 'echo "Disabled: PR preview deploy is handled by firebase-hosting-merge.yml"'
